{% extends 'base.html.twig' %}

{% block navbar %}
	<div class="navbar navbar-expand-lg navbar-light bg-primary">
		<div class="container">
			<a class="navbar-brand" href="#">{{ app_name }}</a>

			<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
				<div class="navbar-nav">
				  <a class="nav-item nav-link" href="{{ path('app_home') }}">Home</a>
				  <a class="nav-item nav-link" href="{{ path('app_user_profile', { 'paramUsername': app.user.username }) }}">Profile</a>
				  <a class="nav-item nav-link" href="{{ path('app_change_password') }}">Change Password</a>
				  <a class="nav-item nav-link" href="{{ logout_path() }}">Logout</a>
				</div>
			</div>
		</div>
	</nav>
{% endblock %}