{% for label, messages in app.flashes(['Success', 'Danger']) %}
	{% for message in messages %}
	    <div class="alert alert-{{ label|lower }} text-center mb-2">
	        {{ message }}
	    </div>
	{% endfor %}
{% endfor %}